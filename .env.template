# .env - Environment variables for FastAPI project

# Database Configuration
# Asynchronous PostgreSQL database URL. 
# Example: postgresql+asyncpg://user:password@host:port/dbname
# IMPORTANT: Use 'postgresql+asyncpg://' for async SQLAlchemy.
DATABASE_URL="postgresql://neondb_owner:npg_G3vFArTfm7wj@ep-old-mud-ad8wpwg1-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# JWT Authentication Configuration
# A strong secret key for JWT token encryption. Generate a new one, e.g., using `openssl rand -hex 32` or `secrets.token_hex(32)`.
JWT_SECRET_KEY="your-super-secret-jwt-key"
# Algorithm used for JWT signing (e.g., HS256, RS256).
ALGORITHM="HS256"
# Access token expiration time in minutes.
ACCESS_TOKEN_EXPIRE_MINUTES=30

# OAuth / External Authentication (Optional)
# Client ID for your OAuth provider (e.g., Google, GitHub).
# AUTH_CLIENT_ID="your-oauth-client-id"
# Client Secret for your OAuth provider.
# AUTH_CLIENT_SECRET="your-oauth-client-secret"

# FastAPI Server Configuration
# Host for the Uvicorn server (e.g., 0.0.0.0 for all interfaces, 127.0.0.1 for localhost only).
UVICORN_HOST="127.0.0.1"
# Port for the Uvicorn server.
UVICORN_PORT=8000
# Logging level (e.g., INFO, DEBUG, WARNING, ERROR, CRITICAL).
LOG_LEVEL="INFO"

# RAG System Configuration (from previous context, if applicable)
# Qdrant host URL (if using Qdrant for vector database).
# QDRANT_HOST="http://localhost:6333"
# OpenAI API Key (if using OpenAI for RAG or other services).
# OPENAI_API_KEY="your-openai-api-key"
