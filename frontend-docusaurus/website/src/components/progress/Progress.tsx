import React from 'react';
import styles from './Progress.module.css';

const Progress = () => {
    return (
        <div className={styles.progressContainer}>
            <h2>My Progress</h2>
            <p>Progress tracking is currently unavailable.</p>
        </div>
    );
};

export default Progress;